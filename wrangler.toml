name = "box-craft"
main = "src/index.ts"
compatibility_date = "2026-09-22"

[observability]
enabled = true

[[kv_namespaces]]
binding = "LOCATION_BITMAP"
id = "fd681a5957544826b82e98f16df52950"

# Events database, shared across Workers. Not yet provisioned in this
# environment — create with `npx wrangler d1 create boxcraft` (or via
# scripts/setup-cloudflare.mjs) and fill in database_id, or add the
# binding via the Cloudflare dashboard. See specs/product/assumptions.md.
# [[d1_databases]]
# binding = "DB"
# database_name = "boxcraft"
# database_id = ""
# migrations_dir = "db/migrations"
